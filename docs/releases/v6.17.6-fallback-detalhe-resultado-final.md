# v6.17.6 — Fallback de resultado final

## Entrega

- corrige a referência usada pelo fallback que consulta o detalhe de uma partida encerrada quando a listagem da competição ainda não fornece placar completo;
- reutiliza a constante `FOOTBALL_API_BASE` já adotada pelas demais chamadas da integração;
- adiciona teste direto para impedir o retorno de uma base de URL inexistente.

## Preservações

- football-data.org continua como única fonte esportiva;
- frequência de cinco minutos, janela de jogos e limite de chamadas permanecem inalterados;
- Supabase, migrações, estrutura de `jogos` e política de status e placar não foram modificados;
- fechamento de palpites, pontuação, Ranking, resultados encerrados e snapshots competitivos permanecem inalterados.
